'use strict';

const Module = require('../Module.js');

class Bazaar extends Module {
    /**
     * @param {Cephalon} bot 
     */
    constructor(bot, token, name, args) {
        super(bot, token, name, args);


    }
}

module.exports = Bazaar;